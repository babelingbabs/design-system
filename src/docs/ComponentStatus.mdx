import { Meta } from '@storybook/blocks'
import { ComponentStatusTable } from './ComponentStatus'

<Meta title="Overview/Component Status" />

# Component Status

All 27 components are **stable** and ship with WCAG 2.1 AA accessibility support where applicable.

The Accessibility column reflects the compliance level validated against the component's interactive behaviour. Layout and typographic primitives are marked **N/A** as they carry no inherent interactive semantics.

---

<ComponentStatusTable />
